# 設計と検証

`README.md` の補足。設計判断の理由と検証の詳細をまとめたもの。

UN Comtrade から農水産物・食品（HS 1〜24類）の貿易統計を取得し、日本の輸出に関する分析用変数を構築する。

| | |
|---|---|
| 対象品目 | HS 1〜24類の**6桁コード** 1,176件 |
| 分類 | HS (as reported) — 各国が報告した時点の HS 版 |
| 期間 | 2012〜2025年（14年） |
| フロー | 輸出（Export） |
| 報告国 / 相手国 | 全報告国 / 全相手国 |
| 規模 | **12,873,073 行**（gzip 約276MB） |

---

## 1. 構成

| ファイル | 役割 |
|---|---|
| [`fetch_ch01-24.ipynb`](fetch_ch01-24.ipynb) | **データ取得。** API から取得し、年ごとファイルまで作る |
| [`build_variables.ipynb`](build_variables.ipynb) | **変数構築。** 取得データから分析用の変数を作る |
| [`comtrade_start.ipynb`](comtrade_start.ipynb) | 単発の確認用。品目1つを指定して素早く中身を見る |
| `README.md` / `README.pdf` | このドキュメント |

取得データ（`data/`）は容量が大きいため**リポジトリに含めない**。
`fetch_ch01-24.ipynb` を実行すれば再生成できる。

### データの流れ

```
UN Comtrade API
    |   fetch_ch01-24.ipynb  セクション5
    v
data/ex_ch01-24_2012-2025_allp/ch{類}_{年}.csv.gz     336ブロック
    |   fetch_ch01-24.ipynb  セクション7
    v
data/by_year_allp/trade_{年}.csv.gz                   14ファイル
    |   build_variables.ipynb
    v
data/variables/{変数名}.csv                           分析用変数
```

---

## 2. セットアップ

### 必要なもの

| | |
|---|---|
| Python | 3.9 以上（動作確認は 3.11 / Anaconda） |
| ライブラリ | `comtradeapicall` `pandas` `python-dotenv` `jupyterlab` |
| API キー | UN Comtrade の **Premium サブスクリプションキー**（32文字） |

```bash
pip install comtradeapicall pandas python-dotenv jupyterlab
```

### API キーの取得

1. https://comtradedeveloper.un.org/ でアカウント登録
2. **Products** から Premium 系のプランを Subscribe
3. **Profile** に表示される Primary key / Secondary key（機能差なし、ローテーション用）

> **無料枠では動かない。** 無料の preview 系 API は1リクエスト500レコード・1期間のみで、
> このリポジトリが使う `getFinalData` / `getCountFinalData` は 401 を返す。
> 所属機関が UN Comtrade の機関契約を持っている場合はそちら経由で取得できることがある。

### キーの登録

キーはノートブックに直接書かない（`.ipynb` は出力ごと保存されるため、共有時に漏れる）。

```bash
read -rs "key?Primary key を貼り付けて Enter: " \
  && printf 'export COMTRADE_KEY=%s\n' "$key" > ~/.comtrade_env \
  && chmod 600 ~/.comtrade_env && unset key && echo "保存しました"
```

`read -rs` を使う理由は3つ。入力が画面に表示されない / 引数ではなく標準入力なのでシェル履歴に残らない /
`unset` で変数からも消える。

確認：

```bash
source ~/.comtrade_env && echo "長さ: ${#COMTRADE_KEY} 文字"   # → 32 文字
```

ノートブックは `python-dotenv` でこのファイルを読む（`export ` 接頭辞は自動で解釈される）。

---

## 3. データ取得 — `fetch_ch01-24.ipynb`

**設定セル（セクション2）だけ書き換えて、上から順に実行する。**

```python
YEARS    = [str(y) for y in range(2012, 2026)]     # 2012〜2025年
FLOW     = "X"                                     # 輸出のみ
CHAPTERS = [f"{i:02d}" for i in range(1, 25)]      # 1〜24類
PARTNER  = None                                    # None=全相手国 / "0"=World のみ
```

| 変数 | 意味 | 書き方 |
|---|---|---|
| `YEARS` | 対象年 | `["2025"]` / `[str(y) for y in range(2000, 2026)]` |
| `CHAPTERS` | 対象の類（2桁ゼロ埋め） | `["01"]` / `["01","02"]` |
| `FLOW` | 貿易フロー | `"X"` 輸出 / `"M"` 輸入 |
| `PARTNER` | 相手国 | `None` 全相手国 / `"0"` World のみ |

出力先は `CHAPTERS` / `YEARS` / `PARTNER` から自動生成される。

```
data/ex_ch01-24_2012-2025_allp/     全相手国版
data/ex_ch01-24_2012-2025_world/    World のみ版
```

**`PARTNER` を出力先名に含めているのは意図的。** World版と全相手国版は行数が1桁違うため、
同じフォルダに混ざると再開判定が古い条件のファイルを「取得済み」と誤認してスキップしてしまう。

### 中断と再開

**セクション5のセルをもう一度実行するだけ。** 保存済みブロックは自動でスキップされる。
カーネルを再起動した場合はセル1〜4を先に実行すること。

API のクォータ切れもトレースバックにならず、進捗を表示して停止する。

```
■ 中断: 呼び出し回数のクォータ切れ。復活まで 16:39:46
  → クォータ復活後にこのセルを再実行すれば続きから走る。
進捗: 210/336 ブロック（残り 126）
```

特定のブロックを取り直すときは該当ファイルを消してから再実行する。

```bash
rm data/ex_ch01-24_2012-2025_allp/ch03_2018.csv.gz
```

### 規模の目安（実測ベース）

| 設定 | 件数 | 容量(gzip) |
|---|---|---|
| 1〜24類 / 2025年 / World のみ | 約 89,500 | 2MB |
| 1〜24類 / 2012〜2025年 / World のみ | 933,662 | 21MB |
| **1〜24類 / 2012〜2025年 / 全相手国** | **12,873,073** | **276MB** |
| 全類 / 2000〜2025年 / 全相手国 | 約 1.9億 | 20〜30GB |

最後の行は API 経由では非現実的（クォータ制限で数日〜）。その規模が必要ならバルクダウンロード契約を検討する。

### 年ごとファイルの作成（セクション7）

取得結果は `ch{類}_{年}.csv.gz`（類 × 年）の粒度なので、年ごとに結合して
`data/by_year_allp/trade_{年}.csv.gz` を作る。`build_variables.ipynb` はこれを入力にする。

各ブロック内は `cmdCode` 昇順だが、24類を単純に連結すると類の境界で並びが切れるため、
結合後に `cmdCode` → 報告国 → 相手国 で並べ直す。

---

## 4. 出力データの仕様

### 列（18列）

| 列 | 内容 |
|---|---|
| `refYear` / `period` | 対象年 |
| `reporterCode` / `reporterDesc` | 報告国（この統計を報告した国） |
| `flowDesc` | Export / Import |
| `partnerCode` / `partnerDesc` | 相手国。`0` / `World` は全世界合計 |
| `classificationCode` | 報告に使われた HS 版（`H0`=1992 … `H6`=2022） |
| `cmdCode` / `cmdDesc` | HS 品目コードと品名 |
| `qty` / `qtyUnitAbbr` / `altQty` | 数量と単位 |
| `netWgt` / `grossWgt` | 純重量 / 総重量（kg） |
| `cifvalue` / `fobvalue` | CIF価額 / FOB価額（USD） |
| `primaryValue` | 主価額（USD）。輸出なら FOB、輸入なら CIF |

### 読み込み時の注意

```python
df = pd.read_csv(path, dtype={"cmdCode": str, "classificationCode": str})
```

`dtype` の指定は必須。省くと `0201` が `201` になり、**先頭ゼロが落ちて品目コードとして使えなくなる**。

数量単位の `"N/A"`（数量未報告を表す API の値）を文字列のまま残したい場合：

```python
df = pd.read_csv(path, dtype={"cmdCode": str}, keep_default_na=False, na_values=[""])
```

既定では pandas が `"N/A"` を欠損として解釈するため、「単位不明」と「欠損」が区別できなくなる。

### 欠損の実情（World のみ版 933,662行での実測）

| 列 | 欠損率 | 理由 |
|---|---|---|
| `cifvalue` | 81.2% | 輸出データなので当然（CIF は輸入側の評価額） |
| `grossWgt` | 31.1% | 報告国が出さないことがある |
| `altQty` | 21.2% | 同上 |
| `qtyUnitAbbr` | 2.6% | 数量未報告（`"N/A"`） |
| `primaryValue` | 0% | 金額は必ず入る |

**輸出額は `fobvalue` または `primaryValue` を使う。**

---

## 5. 変数構築 — `build_variables.ipynb`

`data/by_year_allp/trade_{年}.csv.gz` を入力に、分析用の変数を作る。

### 変数一覧

| 変数名 | 定義 | 粒度 |
|---|---|---|
| `japancountry` | **日本から輸入国 *m* への輸出額の合計。** 日本を報告国、*m* を相手国とする行を HS 1〜24類の全品目で合算 | 年 × 相手国 |
| `worldcountry` | **世界各国から輸入国 *m* への総輸出額。** 全報告国を対象に *m* への行を全品目で合算。*m* の輸入市場規模 | 年 × 相手国 |
| `japanx` | **日本が *m* へ輸出している品目に限定した、世界各国から *m* への輸出額。** その年に日本が *m* へ売っている HS6桁の集合を特定し、その品目について全報告国 → *m* を合算。日本が競合している到達可能な市場規模 | 年 × 相手国 |

`japanx` には補助列 `n_cmd`（日本が *m* へ輸出している品目数）が付く。
`japanx` の解釈には品目数が必要なため。2025年の実測では中央値15品目、最大423品目（香港）、
25パーセンタイルは3品目と**分布が大きく偏っている**。

いずれの変数も `partnerDesc == "World"`（全世界合計の行）は除外して集計する。
残すと個別相手国の合計と二重計上になる。

### 定義上成り立つ関係

```
japancountry  ≤  japanx  ≤  worldcountry
（日本→m）      （世界→m、        （世界→m、
                 日本の品目のみ）    全品目）
```

ノートブックはこれを全行で検査し、崩れていれば `RuntimeError` で停止する。

### 出力形式

各変数を3つの形で保存する。

| 形 | ファイル | 使いどころ |
|---|---|---|
| 年ごとに1ファイル | `{変数名}/{変数名}_{年}.csv` | その年だけ扱いたいとき |
| 横持ち（相手国 × 年） | `{変数名}_wide.csv` | 国ごとの推移を横に並べて見たいとき |
| 縦持ち（年 × 相手国） | `{変数名}.csv` | 回帰などパネル分析にそのまま使う |

### `japanx` の品目集合 — 年ごとか固定か

既定は**年ごと**（`FIXED_BASKET = False`）。日本の品目構成は年で変わるため
（`(相手国, 品目)` の組が2012年 4,959 → 2025年 7,590）。

ただし年ごとにすると `japanx` の時系列変化に「市場自体の変化」と「日本の品目構成の変化」が混ざる。
中国の実測値がその例（単位10億USD）。

| 年 | 年ごと | 固定バスケット |
|---|---|---|
| 2012 | 23.0 | 69.3 |
| 2014 | **66.2** | 77.2 |
| 2015 | **28.4** | 74.1 |
| 2023 | **109.8** | 136.3 |
| 2024 | **38.6** | 112.4 |

年ごとは3倍近く乱高下する。入れ替わった少数品目の市場規模が桁違いなため。
時系列比較をするなら `FIXED_BASKET = True`（全期間で一度でも輸出した品目に固定）を検討する。

---

## 6. 設計思想

このコードの複雑さの大半は、**UN Comtrade API の4つの落とし穴**に対処するためのもの。
いずれも「エラーを出さずに間違ったデータを返す」種類の問題で、素直に書くと気づかないまま壊れる。

### 6.1 100,000件の暗黙の打ち切り

`getFinalData` は `maxRecords` に何を指定しても **1リクエスト100,000件で打ち切る**。警告も例外も出ない。

実測（日本・2022年・全品目・全パートナー・輸出）:

| | 件数 |
|---|---|
| `maxRecords` に指定した値 | 250,000 |
| `getCountFinalData` が返す真の件数 | 177,693 |
| `getFinalData` が実際に返した件数 | **100,000**（77,693件が黙って欠落） |

**対策** — 打ち切りは**ちょうど100,000件**で起きるので、取得結果が `CAP` 未満なら欠落なしと判断できる。
`CAP` に達した場合のみコード列を二分割して取り直す。

```python
if got >= CAP:
    # 上限ちょうど = 打ち切りの可能性 → 分割して取り直す
```

> 当初は取得前に毎回 `getCountFinalData` で真の件数を確認していたが、
> リクエスト数が2倍になりクォータを早く使い切るため廃止した。
> `count_of()` 関数は検証用に残してある。

### 6.2 URL 2000文字の上限

`cmdCode` にコードを並べすぎるとリクエストが弾かれる。

```
Request URL exceeds maximum allowed length (2000 characters).
```

第03類（魚介類）は282コードあり、`cmdCode` 文字列だけで1,973文字になって実際に失敗した。

**対策** — `CODE_STR_MAX = 1_200` を超えたらコード列を二分割する。1コード7文字なので約170コードが目安。
件数による分割と同じ再帰関数で処理しており、**文字数と件数の両方が収まるまで**分割が続く。
2000 ではなく 1200 にしているのは、URL には他のパラメータも載るため。

### 6.3 呼び出し回数のクォータ

使い切ると 403 が返り、**復活まで十数時間**かかる。しかもライブラリは例外を投げず、
メッセージを標準出力に `print` して `None` を返すだけなので、戻り値だけ見ていると原因が分からない。

```
{ "statusCode": 403, "message": "Out of call volume quota. Quota will be replenished in 16:39:46." }
```

**対策** — 標準出力を捕まえて 403 を検出し、専用の例外 `QuotaExceeded` にする。
実行セルはこれを捕まえて、進捗を表示して停止する（トレースバックにしない）。

```python
def _call(fn, *args, **kwargs):
    buf = io.StringIO()
    with contextlib.redirect_stdout(buf):
        res = fn(*args, **kwargs)
    msg = buf.getvalue().strip()
    if "403" in msg or "call volume quota" in msg:
        raise QuotaExceeded(...)
    return res
```

クォータ状態を単体で確認したい場合も同じ仕組みで調べられる。

### 6.4 内訳行の混入による二重計上

`partner2Code` / `customsCode` / `motCode` を `None` にすると、**合計行と一緒に内訳行が全部返る**。
輸送モード別（Road / Railway / …）、通関手続別（Clearance for home use / Outward processing / …）、
原産国別の内訳がそれぞれ別行として混ざる。

実測（0201.10・2022年・輸入・全報告国）:

| 指定 | 行数 | 重複 | 報告国88に対する World 行 |
|---|---|---|---|
| 3つとも `None` | 2,613 | 2,354 | 734 |
| `customsCode` / `motCode` のみ指定 | 946 | 601 | 237 |
| **3つとも合計コードを指定** | **551** | **0** | **88** |

スペイン1国だけで World 行が12行返り、そのまま合計すると**4倍以上に膨れる**。

**対策** — 合計コードを固定値としてハードコードし、設定セルの外に置いた。誤って触れないようにするため。

```python
partner2Code="0"      # World
customsCode="C00"     # TOTAL CPC
motCode="0"           # TOTAL MOT
```

---

## 7. その他の設計判断

### なぜ `AG6` ではなく明示的なコード列を送るのか

`cmdCode="AG6"` は「全6桁コード」を意味する集計指定子だが、**類を限定できない**。
1〜24類だけが必要なので、`getReference("cmd:HS")` から `aggrLevel==6` を抽出して明示的に列挙している。

なお `cmdCode="02"` のように2桁を指定すると、**その2桁の集計行1件だけ**が返り、配下の6桁は取れない。

### なぜ「年 × 類」を取得単位にするのか

再開の粒度と分割の効きやすさのバランス。年だけを単位にすると1ブロックが大きすぎ、
コード単位にするとリクエスト数が爆発する。

6桁コードの分布は極端に偏っている（84類=597号 に対し 45類=7号、77類=0号）ため、
類ごとに区切ると自然に負荷が平準化される。

### なぜ `clCode="HS"`（as reported）なのか

各国が報告した時点の HS 版をそのまま使う設定。UN による版変換を経ないため原データに忠実。

代償として、**版をまたぐとコード体系が変わる**。例えば馬の純血種は:

| 版 | コード |
|---|---|
| HS1992〜2007 | `010110` |
| HS2012以降 | `010121` / `010129` に分割 |

そのため2025年のデータに `010110` は1行も存在しない（44コード送って34コードしか返らないのは正常）。
`classificationCode` 列で報告版を判別できる。

時系列で品目を追う場合は `CL` を `"H6"`（HS2022固定）等に変える選択肢もあるが、変換誤差が入る。

### なぜ失敗時に例外で止めるのか

この API は**失敗時に例外ではなく `None` や空 DataFrame や不完全なデータを返す**。
「エラーが出なかった＝成功した」が成り立たない。

そのため件数・列の存在・重複・大小関係をすべて明示的に検査し、
1つでも崩れたら停止する方針にしている。
**黙って不完全なデータが下流に流れるより、止まって気づけるほうがよい。**

---

## 8. 検証

3段階で確認している。

### ① 内部整合性

| 検査 | 結果 |
|---|---|
| `japancountry ≤ japanx ≤ worldcountry` | 全2,088行で成立 |
| `World` 行の合計 = 相手国別の合計 | 全14年で差の率 0.0% |
| `n_cmd` = 日本の品目数 | 157か国で一致 |
| 取得ブロックの重複行 | 0 |

### ② 別経路での再計算

年ごとファイルを経由せず、元の336ブロックから直接計算し直した（2022年）。

```
japancountry   国数 149 / 最大差 0.000000
worldcountry   国数 243 / 最大差 0.000002
japanx         国数 149 / 最大差 0.000004
```

差は最大 4×10⁻⁶ USD。値の桁が 10¹¹ なので相対誤差 10⁻¹⁷、加算順序による浮動小数点誤差。

### ③ API への問い直し（最も強い検証）

**別のクエリ形式**で取り直して突き合わせた。本体は「1〜24類の6桁を類ごとに列挙して336回取得」だが、
検証では `cmdCode="AG6"` で日本→中国を1リクエストで取得し、ローカルで1〜24類に絞って合計した。

```
API 取得（AG6、1リクエスト）1〜24類 合計 : 1,875,028,218.75 USD
japancountry(2022, China)                : 1,875,028,218.75 USD
差 0.00 USD / 相対誤差 0.00000000 %
```

①②は自分の計算の内部整合性しか見ないので、取得段階の系統的な誤りには気づけない。
③は元のソースに別ルートで問い直すため、パイプライン全体を検証できる。

### 分割ロジックの等価性

44コードを一括送信した結果と、1コードずつ44回取得した結果を突き合わせ、
**全47列・1,109行が完全一致**することを確認した（第1類・2025年・World のみ）。

---

## 9. データ利用上の注意

- **直近年は報告が出揃っていない。** 報告国数は2023年165 → 2024年138 → 2025年93 と落ちる。
  2025年の行数は他年の7割程度。時系列比較でそのまま使うと過小評価になる。
- **`worldcountry` は m の輸入額そのものではない。** 各国が申告した輸出額（FOB）の積み上げなので、
  m 自身が申告する輸入額（CIF）とは一致しない。運賃・保険料の差、報告漏れ、計上時期のズレがあり、
  通常は輸入額のほうが大きい。
- **`Other Asia, nes`（コード490）は台湾。** 参照表では `isGroup=True` だが他国を束ねた集計ではなく
  個別の報告経済。`ASEAN` / `European Union` は報告国として出現しないので、地域集計による二重計上はない。
- **相手国数（242〜244）は報告国数（93〜181）より多い。** 統計を報告していないが
  他国から「そこへ輸出した」と報告されている国・地域があるため。

---

## 10. トラブルシューティング

| 症状 | 原因と対処 |
|---|---|
| `COMTRADE_KEY が未設定` | `source ~/.comtrade_env` を実行、またはカーネル再起動 |
| `401 invalid subscription key` | 無料枠のキーを使っている。Premium が必要 |
| `呼び出し回数のクォータ切れ` | 十数時間待ってセクション5を再実行。続きから走る |
| `Request URL exceeds maximum allowed length` | `CODE_STR_MAX` を下げる（既定1200） |
| `打ち切り: 取得 X < 期待 Y` | 条件を絞る。データを信用しないこと |
| 品目コードの先頭ゼロが消えた | `read_csv` に `dtype={"cmdCode": str}` を指定 |
| 大小関係の `RuntimeError` | 集合の作り方か `World` の除外を確認 |
| 取得が遅い | `PAUSE` を増やす。中断してもセクション5の再実行で続く |
| 学内プロキシ環境 | 各 API 関数に `proxy_url=` 引数を渡す |
